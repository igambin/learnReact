import React from 'react';
import classes from './ExpenseFilterControl.css';

const ExpenseFilterControl = (props) => {

  return (
    <div className={classes.ExpenseFilterControl}>
    </div>
  );
};

export default ExpenseFilterControl;